{
  "mcpServers": {
    "{{SERVER_NAME}}": {
      "type": "stdio",
      "command": "{{COMMAND}}",
      "args": {{ARGS_ARRAY}},
      "env": {
        "{{ENV_VAR_NAME}}": "${{{ENV_VAR_NAME}}}"
      }
    }
  }
}
