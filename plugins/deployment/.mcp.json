{
  "$schema": "https://modelcontextprotocol.io/schema/mcp.json",
  "mcpServers": {
    "sentry": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sentry"
      ],
      "env": {
        "SENTRY_ORG_SLUG": "${SENTRY_ORG_SLUG}",
        "SENTRY_PROJECT_SLUG": "${SENTRY_PROJECT_SLUG}",
        "SENTRY_AUTH_TOKEN": "${SENTRY_AUTH_TOKEN}"
      },
      "description": "Sentry error tracking and monitoring - query issues, create alerts, analyze error trends. Set SENTRY_ORG_SLUG, SENTRY_PROJECT_SLUG, SENTRY_AUTH_TOKEN in environment or Doppler."
    }
  }
}
