{
  "$schema": "https://modelcontextprotocol.io/schema/mcp.json",
  "mcpServers": {
    "sentry": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sentry"
      ],
      "env": {
        "SENTRY_AUTH_TOKEN": "${SENTRY_AUTH_TOKEN}"
      },
      "description": "Global Sentry MCP server - list organizations, list projects, create projects, query issues, create alerts. Agents select project dynamically. Auth token from Doppler: SENTRY_AUTH_TOKEN"
    }
  }
}
