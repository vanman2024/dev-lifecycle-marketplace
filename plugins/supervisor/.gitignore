# ====================
# CRITICAL SECURITY
# ====================
# Prevent API keys and secrets from being committed

# Environment Variables (NEVER commit actual .env files!)
.env
.env.local
.env.*.local
*.env
!.env.example
!.env.template

# MCP Server Configurations with REAL API Keys
# Template .mcp.json is safe, but protect local configs with real keys
.mcp.local.json
.mcp.*.local.json

# Credentials and Secrets
secrets/
credentials/
*.pem
*.key
*.crt
*.p12
*.keystore
**/api-keys/
**/tokens/
*.secret

# Local Configuration (may contain keys)
config.local.*
settings.local.*

# OS Files
.DS_Store
Thumbs.db
desktop.ini

# IDE Files
.vscode/
.idea/
*.sublime-*

# Logs (may contain sensitive data)
*.log
logs/

# Temporary & Backup Files (may contain old secrets)
tmp/
temp/
*.tmp
*.temp
*.bak
*.backup
*~
*.swp
*.swo

# Node.js
node_modules/

# Python
__pycache__/
*.pyc
venv/
env/

# User-specific data (may contain keys)
user-data/
local-data/
*.local
